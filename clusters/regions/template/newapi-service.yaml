apiVersion: v1
kind: Service
metadata:
  name: newapi
  labels:
    app: newapi
    region: "${REGION}"
spec:
  type: ClusterIP
  selector:
    app: newapi
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
